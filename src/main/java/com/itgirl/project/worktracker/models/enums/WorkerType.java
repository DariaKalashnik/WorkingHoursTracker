package com.itgirl.project.worktracker.models.enums;

/*
  Enum to determine type of user.
  If user is OFFICE type, the timer will start
  only in the specific network
*/
public enum WorkerType {
    OFFICE,
    HOME;
}
